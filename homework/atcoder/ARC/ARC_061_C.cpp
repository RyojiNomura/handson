#include <bits/stdc++.h>
using namespace std;

int digit_sum(string &str, int i){
    substr = str.substr(0, i);

    if(i=1){
        int num = atoi(substr.c_str());
        return num
    }

    int sum;

    sum = digit_sum(str, i-1);


    
}

int main(){
    string s;
    cin >> s;
    n = s.size();
    int sum;
    sum = 

}